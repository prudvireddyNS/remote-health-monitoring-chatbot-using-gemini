from main import cursor
#cursor.execute("CREATE TABLE patients(patient_id  INT PRIMARY KEY AUTO_INCREMENT,patient_name VARCHAR(30),joining_date DATE,discharge_date DATE, symptoms varchar(150), prev_diagnosis VARCHAR(300),new_symptoms VARCHAR(150), latest_diagnosis VARCHAR(300)   )")
# cursor.execute("select * from patients")
# select = cursor.fetchall()
# print(type(select))
# print(cursor.rowcount)